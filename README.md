# Test Repo

This repository now includes a simple command line calculator written in Python.

## Usage

Run the calculator with:

```bash
python calculator.py
```

Follow the on-screen prompts to perform calculations.
